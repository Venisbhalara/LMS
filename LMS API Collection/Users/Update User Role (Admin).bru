meta {
  name: Update User Role (Admin)
  type: http
  seq: 4
}

put {
  url: http://localhost:5000/api/users/1/role
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
  Content-Type: application/json
}

body:json {
  {
    "role": "instructor"
  }
}

settings {
  encodeUrl: true
}
