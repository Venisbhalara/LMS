meta {
  name: Delete User (Admin)
  type: http
  seq: 5
}

delete {
  url: http://localhost:5000/api/users/1
  body: none
  auth: inherit
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}
